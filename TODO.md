- [ ] Step 1: Add Flutter widget tests for high-risk flows (CartScreen, CheckoutScreen, PaymentResultScreen).
- [ ] Step 2: Refactor Flutter screens/services to allow test overrides (remove hard-coded static service calls or add service locator/override layer used by screens).
- [ ] Step 3: Implement mocks/stubs for:
  - CartService: fetchCart, updateItemQuantity, removeItem/removeItems, clearCart
  - AuthService: getCurrentUser
  - CheckoutService: fetchPaymentConfig, fetchShippingConfig, fetchLoyaltyData, applyCoupon, createOrder, createPaymentLink, cancelOrder
  - VoucherService (only if needed for tests touching voucher modal)
- [ ] Step 4: Write widget tests:
  - CartScreen: loading/empty/content, selection & checkout navigation
  - CheckoutScreen: confirm button enabled only after policy acceptance, coupon apply success/failure UI, COD submit path navigation
  - PaymentResultScreen: UI for success/failure and button behavior
- [ ] Step 5: Run `flutter test` and fix any test/environment issues (assets/images/network).
